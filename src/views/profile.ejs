<p>
    ID:
    <%= user._id %><br /> Username:
        <%= user.username %><br /> Name:
            <%= user.displayName %><br /> Password:
                <%= user.password %><br />
                    <% if (user.emails) { %> Email:
                        <%= user.emails[0].value %><br />
                            <% } %>
</p>
<a href="/logout">Log out</a>